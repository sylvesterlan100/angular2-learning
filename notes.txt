
When using the [src] or [`name`] syntax,
Updating the property value, not the attribute value!

The attribute value is a one time initialization.

In databinding, we always deal with properties!

[] = properties
() = event binding
* = structural